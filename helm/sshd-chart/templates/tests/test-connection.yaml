apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sshd-chart.fullname" . }}-test-connection"
  labels:
    {{- include "sshd-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sshd-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
